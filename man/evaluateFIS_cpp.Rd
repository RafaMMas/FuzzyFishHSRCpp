% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R, R/evaluateFIS_cpp.R
\name{evaluateFIS_cpp}
\alias{evaluateFIS_cpp}
\title{Evaluate Fuzzy Inference System (FIS) Using C++}
\usage{
evaluateFIS_cpp(
  inputData,
  membershipFunctions,
  ruleConsequents,
  inputLimits = NULL
)

evaluateFIS_cpp(
  inputData,
  membershipFunctions,
  ruleConsequents,
  inputLimits = NULL
)
}
\arguments{
\item{inputData}{A data frame or matrix containing the input data to be evaluated by the FIS.}

\item{membershipFunctions}{A list or matrix of fuzzy membership functions corresponding to the input data.}

\item{ruleConsequents}{A list or matrix of fuzzy rule consequents, which are the results of the rules applied to the input data.}

\item{inputLimits}{(optional) A list or vector specifying the limits for the input data. Default is \code{NULL}.}
}
\value{
A numeric matrix where the first column contains the computed output values, and the remaining columns indicate the degree of membership to each fuzzy rule.

A numeric vector of output values resulting from the evaluation of the fuzzy inference system.
}
\description{
This function evaluates a fuzzy inference system (FIS) using C++ for improved computational efficiency.
It processes input data through specified membership functions and rule consequents, applying fuzzy logic to compute outputs.

This function evaluates a fuzzy inference system (FIS) based on the provided input data,
membership functions, and rule consequents. It processes the inputs using fuzzy logic and
returns the evaluated output.
}
\details{
The function utilizes fuzzy logic to evaluate input data against predefined membership functions and rule consequents.
The output includes both the computed fuzzy output and rule memberships, aiding interpretability in fuzzy inference systems.
}
\examples{
# Example usage:
result <- evaluateFIS_cpp(inputData, membershipFunctions, ruleConsequents, inputLimits)
}
